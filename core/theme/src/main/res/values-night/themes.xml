<resources>
    <!-- Base application theme. -->
    <style name="BaseTheme.Night"  parent="BaseTheme">
        <!-- Colors -->
        <!-- Background color -->
        <item name="android:colorBackground">@color/black_850</item>
        <item name="colorOnBackground">@color/black_100</item>
        <!-- Surface color -->
        <item name="colorSurface">@color/black_800</item>
        <item name="colorOnSurface">@color/black_100</item>
        <!-- Outline color -->
        <item name="colorOutline">@color/black_400</item>
    </style>

    <style name="Theme.Budgetify" parent="BaseTheme.Night" />
</resources>