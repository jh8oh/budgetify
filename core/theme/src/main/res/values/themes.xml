<resources>
    <!-- Base application theme. -->
    <style name="Theme.Budgetify" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color -->
        <item name="colorPrimary">@color/teal_500</item>
        <item name="colorPrimaryVariant">@color/teal_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color -->
        <item name="colorSecondary">@color/maroon_700</item>
        <item name="colorSecondaryVariant">@color/maroon_900</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Background color -->
        <item name="android:colorBackground">@color/white</item>
        <item name="colorOnBackground">@color/black_900</item>
        <!-- Text color -->
        <item name="android:textColorPrimary">@color/color_text_primary</item>
        <!-- Outline color -->
        <item name="colorOutline">@color/black_700</item>
    </style>

    <style name="Theme.Budgetify.NoActionBar" parent="Theme.Budgetify">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
</resources>